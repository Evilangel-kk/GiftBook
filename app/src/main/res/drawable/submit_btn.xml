<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <padding
        android:left="20dp"
        android:right="20dp"
        android:top="15dp"
        android:bottom="15dp"  />
    <corners android:radius="25dp" />
    <!-- 输入框背景颜色 -->
    <solid android:color="@color/submitBtnBg"></solid>
    <!-- 外边框线的粗细及其颜色 -->
    <stroke
        android:width="5dp"
        android:color="@color/editTextStroke" >
    </stroke>
</shape>